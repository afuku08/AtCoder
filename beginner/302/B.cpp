#include <bits/stdc++.h>

using namespace std;

int main(){
    int h, w;
    cin >> h >> w;
    vector<string> s(h);
    for(auto &a : s) cin >> a;

    for(int i=0;i<h;i++){
        if(s[i].find("snuke") != string::npos  || )
    }

    for(int i=0;i<h;i++){
        for(int j=0;j<w;j++){
            if(s[i][j] == '')
        }
    }
}