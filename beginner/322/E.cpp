#include <bits/stdc++.h>
using namespace std;



bool func(int i, int k, const vector<int>& c,)

int main(){
    int n, k, p;
    cin >> n >> k >> p;
    vector<int> c(n);
    vector<vector<int>> a(n, vector<int>(k));
    for(int i=0;i<n;i++){
        cin >> c[i];
        for(auto&& ain : a[i]) cin >> ain;
    }


}